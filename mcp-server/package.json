{
  "name": "pii-firewall-mcp-server",
  "version": "1.0.0",
  "description": "Local MCP server that detects, redacts and restores Taiwanese PII so cloud LLM agents never see the original data.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test 'test/*.test.js'",
    "smoke": "node scripts/smoke.js ../samples/resume_01.txt",
    "prefetch-model": "node scripts/prefetch-model.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.24.0"
  }
}
